/** * Test Sound using automated SoundTester * * @author (C) 1997 Phil Burk, All Rights Reserved */package workshop;import java.util.*;import java.awt.*;import java.applet.Applet;import com.softsynth.jsyn.*;import com.softsynth.jsyn.view102.*;import com.softsynth.jsyn.circuits.*;import JSynExamples.*;public class WS_TestSound extends SoundTestApplet{/* Can be run as either an application or as an applet. */    public static void main(String args[])	{		WS_TestSound applet = new WS_TestSound();		AppletFrame frame = new AppletFrame("Test Sounds", applet);		frame.resize(600,400);		frame.show();/* Begin test after frame opened so that DirectSound will use Java window. */		frame.test();	}	/** Override this method to make an Applet that tests a different circuit. */	public SynthCircuit makeCircuit() throws SynthException	{//		return new Beeper();//		return new BeeperVibrato();//		return new BeeperVibratoEnv();//		return new patches.SampleHoldSaw();		return new patches.BerlinBeeper();	}}