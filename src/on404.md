---
layout: base.njk
---

&lt;!-- .style1 {color: #FFFFFF} --&gt; 0 ) { $new\_page = $new\_page . '?' . $old\_query; } $message\_php = "It may have been converted to PHP. "; } // TODO - refactor, extract function else if( endsWith( $old\_path, ".htm") ) { $new\_page = substr( $old\_path, 0, strlen($old\_path) - 4) . ".php"; if( strlen($old\_query) > 0 ) { $new\_page = $new\_page . '?' . $old\_query; } $message\_php = "It may have been converted to PHP. "; } //Meta refresh to send to correct page. echo ''; ?> That page could not be found.\\n"; echo "

```text
$old_page
```

\\n"; echo "

$message\_php You will be redirected to a new page in a moment. Please update your bookmarks!

\\n"; echo "

If the redirect does not work then click here: [$new\_page](/\"$new_page\")

\\n"; echo "

Or visit the SoftSynth : [home page](/\"/index.php\")

\\n"; ?>

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!

This is just to fill up the page because otherwise the browser will get confused!